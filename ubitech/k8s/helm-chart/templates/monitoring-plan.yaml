{{- if .Values.monitoringPlan.enabled }}

apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: {{ .Values.monitoringPlan.name }}
  namespace: {{ if .Values.monitoringPlan.namespace }}{{ .Values.monitoringPlan.namespace }}{{ else }}{{ .Release.Namespace }}{{ end }}
spec:
  config: {{ .Values.monitoringPlan.config | quote }}

{{- end }}
